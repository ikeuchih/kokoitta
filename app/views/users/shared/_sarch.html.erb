<h1>serch</h1>

<%= form_tag trend_index_path, method: :get, class: 'searchForm' do %>
      <%= select_tag :tag_id,
                     options_from_collection_for_select(Tag.all, :id, :tag_name, params[:tag_maps_id]),
                     {
                       prompt: 'Which country?',
                       onchange: 'submit(this.form);'
                     }
      %>
      
<% end %>